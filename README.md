# FussFree — Run on Localhost

## Requirements
- Node.js 18+ → https://nodejs.org

## Steps to run
```bash
cd FussFree
npm install
npm run dev
```
Open: **http://localhost:5173**

> react-leaflet is pinned to v4 — fixes the blank screen error.
> Allow location permission in browser for live GPS map.
